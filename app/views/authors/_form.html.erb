<%= form_for @author do |form| %>

  <table>
    <tbody>
      <tr>
        <td><%= form.label :name %></td>
        <td><%= form.text_field :name %></td>
      </tr>

      <tr>
        <td><%= form.label :book %></td>
        <td><%= form.text_field :book %></td>
      </tr>
    </tbody>
  </table>
<%= form.submit "Save" %>
<% end %>
